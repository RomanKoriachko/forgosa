import React from 'react';
import { Marquee } from './components';

type Props = {};

const Main = (props: Props) => {
	return (
		<main className='main'>
			<Marquee />
		</main>
	);
};

export default Main;
