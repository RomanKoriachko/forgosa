import React from 'react';

import './HeaderButton.scss';

type Props = {};

const HeaderButton = (props: Props) => {
	return <button className='header-btn'>book a meeting</button>;
};

export default HeaderButton;
